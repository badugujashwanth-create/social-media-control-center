# Social preview upload

`social-preview.png` is the prepared 1280×640 repository preview.

After the release PR merges and the repository is renamed, open **Repository → Settings → General → Social preview → Edit**, upload this PNG, save, and verify the sharing card. The GitHub CLI metadata command does not upload repository social previews.
